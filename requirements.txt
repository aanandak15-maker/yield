# Core data science libraries
pandas==2.1.4
numpy==1.24.3
scikit-learn==1.3.0
requests==2.31.0
joblib==1.3.2

# Minimal geospatial (only what FastAPI needs)
geopandas==0.14.3
Shapely==2.0.1

# Phase 6: Real-time Prediction Service (Essential Only)
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
python-multipart==0.0.6

# Authentication (minimal)
google-auth==2.23.4

# Reduced API clients (just for basic functionality)
earthengine-api==0.1.384

# Basic plotting (if dashboard needs it)
matplotlib==3.8.0
