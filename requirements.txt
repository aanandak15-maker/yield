# Render Production - Full ML Stack with Compatibility Constraints

# Web Framework
fastapi>=0.104.0,<1.0.0
uvicorn[standard]>=0.24.0,<0.25.0
pydantic>=2.11.0,<3.0.0
python-multipart>=0.0.6

# Machine Learning Dependencies with EXACT VERSION PINNING for Model Reproducibility
# These versions are trained and deployed together to ensure 100% compatibility

scikit-learn==1.3.2         # Exact version for pickle/networking compatibility
numpy==1.24.4                # Exact version matching sklearn compilation
scipy==1.11.3                # Compatible with numpy/scikit-learn
pandas==1.5.3                # Compatible stable version
joblib==1.3.2                # Exact joblib for sklearn serialization

# XGBoost (Required for gradient boosting models) - Exact compiled version
xgboost==1.7.6               # Exact version matching internal module structure

# Lock additional dependencies to prevent compatibility issues
threadpoolctl==3.1.0         # Required by scikit-learn
packaging==23.1              # Required by various packages

# API Client Dependencies
requests>=2.31.0,<3.0.0
earthengine-api>=0.1.389,<0.2.0

# Additional Libraries
flask>=3.0.0,<4.0.0
flask-cors>=4.0.0,<5.0.0

# Data Science Extensions
matplotlib>=3.6.0,<4.0.0
seaborn>=0.12.0,<1.0.0
